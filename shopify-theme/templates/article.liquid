<section class="legal-hero">
  <div class="container legal-wrap"><p class="eyebrow">{{ article.published_at | date: "%B %d, %Y" }}</p><h1>{{ article.title }}</h1></div>
</section>
<section class="legal-body">
  <div class="container legal-wrap rte">{{ article.content }}</div>
</section>
